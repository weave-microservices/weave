// const { Weave } = require('../../lib/index')
// const { createRegistry } = require('../../lib/registry/registry')

describe('Registry', () => {
  it('shout create a registry object', () => {
    // const broker = Weave()
    // const registry = createRegistry()
    // registry.init(broker, )
  })
})
