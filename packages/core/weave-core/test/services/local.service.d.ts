export const name: string;
export namespace actions {
    namespace hidden {
        const visibility: string;
        namespace params {
            const text: string;
        }
        function handler(context: any): any;
        function handler(context: any): any;
    }
}
