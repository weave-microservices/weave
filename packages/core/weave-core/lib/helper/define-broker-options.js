/**
 * Create and register a new service
 * @param {import('../with-type.js').BrokerOptions} options - Broker options
 * @returns {import('../with-type.js').BrokerOptions} Broker options
*/
module.exports = function (options) {
  return options
}
