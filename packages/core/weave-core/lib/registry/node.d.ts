import { Node } from '../shared/interfaces/node.interface';
/**
 * Create a new node with given node ID.
 * @export
 * @param {string} nodeId
 * @returns {Node}
 */
export declare function createNode(nodeId: string): Node;
