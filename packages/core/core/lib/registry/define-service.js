/**
 * Create and register a new service
 * @param {import('./service.js').ServiceSchema} schema - Schema of the Service
 * @returns {import('./service.js').ServiceSchema} Service schema
 */
module.exports = (schema) => {
  return schema
}
